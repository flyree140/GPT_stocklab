# 先看這裡

你可以直接用本專案覆蓋 `flyree140/GPT_stocklab`。

## 上傳前

先備份舊 Repository 或建立 `backup-v15` branch。

## 上傳後

- 確認根目錄看得到 `.github/workflows/`。
- 開啟 GitHub Actions `Read and write permissions`。
- Pages Source 選 GitHub Actions。
- 先執行 `Daily StockLab 16 research`，第一次可 `skip_qwen=true`。

## v16 你會看到

研究總覽 / 股票研究 / 新聞證據室 / 歷史快照 / 試買模擬 / 我的最愛 / 方法設定。
